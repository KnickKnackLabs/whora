#!/usr/bin/env -S uv run --script
#MISE description="Stop a countdown"
#USAGE flag "--id <id>" required=#true help="Countdown id"
#USAGE flag "--json" default=#false help="Print machine-readable JSON"
#USAGE example "mise run countdown:stop --id tea" header="Stop a named countdown"
# /// script
# requires-python = ">=3.10"
# ///

import os
import sys

sys.path.insert(0, os.environ["MISE_CONFIG_ROOT"] + "/lib")

from whora import StopArgs, TimerStore, WhoraError, bool_env, countdown_stop, main_error, normalize_empty


def main() -> int:
    args = StopArgs(
        id=normalize_empty(os.environ.get("usage_id")),
        json_output=bool_env(os.environ.get("usage_json")),
    )
    return countdown_stop(TimerStore.from_env(os.environ), args)


try:
    raise SystemExit(main())
except WhoraError as exc:
    raise SystemExit(main_error(exc)) from None
